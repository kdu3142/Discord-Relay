# Discord Bot Configuration
# Get these from https://discord.com/developers/applications

# Required: Bot token from Discord Developer Portal
DISCORD_TOKEN=your_bot_token_here

# Required: Full URL to your n8n webhook endpoint
# Example: https://your-n8n-instance.com/webhook/discord/bot/abc123xyz
N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/discord/bot/your-webhook-id

# Optional: Discord Application Client ID (for future slash commands)
DISCORD_CLIENT_ID=

# Optional: Shared secret for HMAC signature verification in n8n
# Generate a random string: openssl rand -hex 32
# This should match the secret used in your n8n workflow
RELAY_SHARED_SECRET=

# Optional: Comma-separated list of Discord guild (server) IDs to allow
# If not set, bot will process messages from all servers it's in
# Example: ALLOWED_GUILD_IDS=123456789012345678,987654321098765432
ALLOWED_GUILD_IDS=

# Optional: Command prefix to trigger the bot (default: !bot)
# Example: BOT_PREFIX=!tobias
BOT_PREFIX=!bot

# Optional: Logging level (default: info)
# Options: error, warn, info, debug
LOG_LEVEL=info

# Optional: Node environment (default: production)
# Set to 'development' for more verbose console output
NODE_ENV=production
